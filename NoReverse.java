class NoReverse
{
   public static void main(String args[])
   {
        int a=Integer.parseInt(args[0]);
        int no;
        int total=0;
        int temp=no;

        while (a>0)
        {
               a=a/10;
               total++;
        }
        while (a>0)
        {
            int digit = no%10;
            a[i]=digit;
            i++;
            no=no/10;
        }
        Arrays.sort(a);
   }
}

        